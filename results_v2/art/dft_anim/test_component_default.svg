<?xml version="1.0" encoding="utf-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="720" height="720" viewBox="0 0 720.000000 720.000000">
  <title>art XDoG_Guide_0000 DFT animation</title>
  <style>
    #background { fill: #fbfaf6; }
    #sourceLayer { opacity: 0.18; }
    #tracePath { fill: none; stroke: #087f8c; stroke-width: 2.2; stroke-linecap: round; stroke-linejoin: round; }
    #rebuildPath { fill: none; stroke: rgba(8, 127, 140, 0.22); stroke-width: 1.1; stroke-linecap: round; stroke-linejoin: round; }
    .arm-line { stroke: rgba(184, 107, 37, 0.76); stroke-width: 1; }
    .arm-circle { fill: none; stroke: rgba(184, 107, 37, 0.17); stroke-width: 1; }
    #armHead { fill: #b23a48; stroke: none; }
  </style>
  <rect id="background" x="0" y="0" width="720" height="720"/>
  <g id="sourceLayer">
<g transform="translate(0.000000,720.000000) scale(0.100000,-0.100000)"
fill="#000000" stroke="none">
<path d="M3715 6895 c-149 -13 -281 -31 -330 -45 -16 -5 -57 -13 -90 -19 -33
-5 -77 -17 -97 -25 -21 -9 -46 -16 -57 -16 -26 0 -230 -68 -258 -86 -12 -8
-27 -14 -33 -14 -5 0 -39 -13 -73 -30 -35 -16 -67 -30 -72 -30 -4 0 -33 -15
-64 -34 -31 -18 -77 -45 -102 -59 -59 -32 -91 -50 -103 -59 -154 -110 -320
-269 -430 -413 -38 -49 -75 -95 -83 -101 -7 -6 -13 -14 -13 -16 0 -6 -103
-165 -118 -183 -8 -10 -48 -74 -88 -143 -32 -55 -194 -376 -194 -385 0 -4 -16
-42 -35 -83 -19 -41 -35 -78 -35 -83 0 -5 -6 -22 -14 -38 -16 -32 -27 -64 -62
-178 -14 -44 -32 -93 -40 -108 -8 -16 -14 -33 -14 -39 0 -6 -9 -32 -21 -57
-12 -25 -27 -59 -34 -76 -44 -105 -114 -236 -132 -248 -7 -4 -13 -12 -13 -17
0 -18 -78 -95 -152 -150 -68 -51 -102 -71 -203 -114 -62 -27 -162 -55 -217
-62 -32 -3 -58 -10 -58 -15 0 -14 91 -10 135 6 22 8 48 15 57 15 37 0 231 95
298 146 62 48 170 157 170 173 0 5 6 14 13 18 19 12 105 176 122 233 4 14 17
39 30 55 12 17 27 50 33 75 5 25 16 61 23 80 21 60 45 133 64 190 9 30 33 88
51 128 19 40 34 77 34 83 0 5 4 17 9 27 6 9 20 40 32 67 20 48 27 62 48 103 5
9 20 41 32 70 13 28 27 52 31 52 5 0 8 8 8 18 0 10 8 27 18 38 9 11 26 37 37
59 11 22 34 61 52 87 18 26 33 50 33 53 0 3 16 28 35 55 19 27 54 78 78 113
55 80 127 171 138 175 5 2 9 8 9 13 0 15 173 198 232 244 96 76 194 145 206
145 3 0 13 6 21 14 23 20 212 116 306 155 194 80 195 80 470 149 168 42 484
82 654 82 109 0 380 -34 413 -51 9 -5 26 -9 38 -9 12 0 36 -6 53 -14 76 -35
109 -45 174 -51 66 -7 107 -19 188 -55 22 -10 76 -32 120 -50 44 -17 98 -40
121 -51 23 -10 48 -19 57 -19 8 0 17 -3 19 -7 2 -5 31 -21 65 -38 35 -16 63
-33 63 -37 0 -3 12 -11 28 -17 28 -11 63 -31 72 -42 3 -3 20 -14 38 -24 17
-11 32 -23 32 -27 0 -4 6 -8 14 -8 8 0 16 -3 18 -7 2 -5 21 -20 43 -35 22 -15
55 -41 73 -58 18 -16 36 -30 38 -30 10 0 244 -238 244 -249 0 -5 7 -14 16 -19
20 -11 95 -117 162 -227 68 -112 174 -334 207 -435 10 -30 26 -74 37 -97 10
-24 18 -48 18 -55 0 -7 6 -30 14 -53 8 -22 21 -67 29 -100 9 -33 25 -94 36
-137 12 -42 21 -83 21 -91 0 -9 -21 -19 -57 -27 -32 -7 -75 -22 -96 -32 -41
-21 -100 -74 -110 -100 -3 -10 -14 -18 -24 -18 -10 0 -31 -7 -47 -15 -36 -19
-55 -19 -62 0 -18 45 -195 121 -281 119 -71 -2 -77 -13 -70 -129 4 -62 14
-120 27 -153 11 -29 20 -72 20 -97 0 -47 23 -118 47 -147 15 -17 17 -24 28
-123 9 -86 13 -102 31 -119 21 -22 69 -20 134 5 30 11 69 25 85 31 17 6 46 23
66 39 30 23 43 27 73 22 22 -3 38 -12 41 -24 18 -55 121 -181 169 -206 27 -14
30 -13 58 14 17 15 34 38 39 51 12 32 29 18 29 -24 0 -34 16 -110 47 -229 29
-111 100 -286 146 -360 42 -68 109 -145 128 -145 5 0 9 -4 9 -10 0 -11 66 -48
125 -70 22 -8 42 -19 43 -25 4 -20 -65 -33 -176 -34 -103 -1 -117 1 -162 25
-28 14 -53 30 -56 35 -7 12 -34 12 -34 0 0 -6 18 -20 40 -32 22 -13 40 -27 40
-32 0 -12 -39 2 -72 24 -14 11 -29 19 -32 19 -8 0 -64 49 -95 84 -14 16 -32
35 -38 42 -7 7 -13 17 -13 23 0 6 -4 11 -9 11 -5 0 -14 15 -22 33 -7 17 -16
34 -19 37 -8 6 -50 94 -50 103 0 4 -8 26 -19 50 -19 42 -37 95 -61 175 -7 23
-19 42 -25 42 -8 0 -5 -24 12 -77 37 -120 77 -223 85 -223 5 0 8 -7 8 -16 0
-37 97 -183 166 -249 19 -19 43 -62 64 -115 18 -47 41 -94 51 -106 11 -11 19
-24 19 -28 0 -14 74 -83 93 -87 10 -2 18 -8 17 -14 -1 -13 -73 -10 -125 6 -69
20 -179 71 -217 101 -40 31 -44 16 -6 -20 35 -34 102 -72 128 -72 8 0 29 -8
45 -19 26 -15 33 -29 48 -97 11 -43 25 -101 32 -129 8 -27 21 -79 31 -115 28
-108 54 -203 68 -250 8 -25 21 -70 30 -100 38 -126 118 -362 140 -415 13 -29
30 -73 62 -157 9 -24 25 -61 35 -84 10 -23 19 -46 19 -52 0 -6 7 -23 14 -39
24 -45 83 -251 97 -338 28 -161 14 -382 -32 -516 -10 -31 -19 -61 -19 -68 0
-6 -13 -40 -30 -74 -16 -35 -30 -69 -30 -75 0 -7 -4 -12 -8 -12 -5 0 -15 -12
-23 -27 -21 -42 -82 -127 -96 -136 -17 -10 -17 -27 0 -27 14 0 29 18 88 105
54 81 59 89 65 108 4 9 10 17 15 17 15 0 70 -61 107 -120 38 -58 58 -54 22 5
-21 34 -16 47 8 23 9 -9 12 -9 12 0 0 17 -39 54 -50 47 -13 -8 -40 23 -40 47
0 11 -10 28 -22 38 l-23 18 23 46 c12 26 22 56 22 67 0 11 5 30 11 41 6 12 17
66 25 122 32 239 0 482 -92 694 -8 18 -14 40 -14 48 0 9 -6 28 -14 42 -8 15
-26 56 -41 92 -14 36 -30 73 -36 82 -5 10 -9 24 -9 32 0 8 -6 27 -14 43 -15
30 -25 57 -63 173 -13 41 -31 88 -39 103 -8 16 -14 35 -14 42 0 7 -14 55 -31
107 -17 51 -34 111 -38 133 -11 51 -29 123 -52 205 -28 103 -79 310 -79 323 0
3 29 4 63 3 58 -1 97 12 97 33 0 3 -19 15 -41 26 -41 19 -102 81 -120 120 -5
11 -13 27 -18 35 -23 37 -49 98 -44 103 3 3 24 -5 48 -19 24 -13 58 -25 77
-26 18 -2 36 -6 39 -10 3 -5 33 -11 68 -14 50 -6 61 -4 59 7 -2 10 14 15 57
20 33 4 76 13 94 20 36 13 121 66 121 76 0 3 -15 2 -34 -1 -40 -8 -139 23
-194 60 -44 30 -130 119 -143 148 -5 12 -21 41 -35 66 -38 68 -79 170 -112
285 -35 117 -44 157 -53 225 -4 36 -11 84 -15 108 -6 40 1 78 28 153 13 37 15
111 2 116 -7 2 -13 -19 -17 -57 -3 -33 -10 -67 -16 -74 -7 -8 -14 -38 -18 -67
-13 -111 -78 -201 -122 -167 -24 19 -49 47 -94 106 -34 45 -41 62 -41 100 -1
42 1 47 19 44 11 -2 32 -4 48 -3 17 0 27 -5 27 -14 0 -30 52 -17 87 21 37 41
38 46 6 38 -12 -4 -23 -16 -25 -28 -2 -15 -11 -22 -33 -24 -24 -3 -28 -1 -17
9 22 23 13 29 -48 29 l-60 0 0 30 c0 23 7 35 30 49 39 23 120 33 188 22 43 -7
52 -6 48 5 -3 7 3 18 12 23 15 9 17 28 14 173 -1 104 -8 190 -19 238 -16 73
-43 120 -69 120 -12 0 -44 91 -44 127 0 16 -19 83 -52 178 -5 17 -14 46 -18
65 -9 38 -22 78 -50 150 -10 25 -26 65 -35 90 -10 25 -23 59 -30 75 -8 17 -31
71 -52 120 -21 50 -58 119 -81 155 -24 36 -48 75 -53 88 -5 12 -13 22 -19 22
-5 0 -10 5 -10 11 0 32 -213 271 -347 390 -53 46 -207 164 -248 189 -22 13
-42 26 -45 29 -5 6 -19 14 -99 59 -25 14 -55 31 -66 38 -11 8 -26 14 -35 14
-8 0 -26 9 -40 20 -14 11 -31 20 -38 20 -8 0 -30 7 -50 16 -21 9 -52 22 -70
30 -18 8 -37 14 -43 14 -6 0 -23 8 -37 18 -39 27 -216 82 -263 82 -22 0 -56 7
-77 16 -38 16 -125 44 -202 64 -62 17 -308 40 -410 38 -47 0 -148 -6 -225 -13z
m2763 -2157 c22 -46 32 -105 32 -199 0 -53 4 -119 10 -147 10 -54 0 -110 -22
-124 -7 -4 -23 -8 -35 -8 -24 0 -140 37 -158 50 -5 4 -21 11 -34 15 -47 14
-66 42 -63 92 2 48 15 73 39 73 16 0 68 33 96 62 24 25 45 23 22 -2 -10 -11
-15 -20 -11 -20 13 0 62 29 75 43 10 13 9 16 -7 21 -31 10 -134 -4 -159 -22
-36 -27 -26 -34 16 -12 22 11 42 16 45 11 4 -5 -14 -19 -39 -31 -25 -12 -47
-26 -51 -31 -8 -14 -24 0 -24 22 0 11 -9 38 -20 59 l-20 40 20 20 c11 11 20
25 20 30 0 6 5 10 12 10 6 0 20 8 30 17 23 20 127 50 179 52 28 1 38 -4 47
-21z m-585 -79 c34 -17 70 -38 79 -47 10 -9 26 -25 36 -34 22 -21 45 -88 30
-88 -5 0 -22 8 -36 18 -15 10 -48 24 -74 31 -43 12 -48 12 -77 -10 -35 -26
-35 -38 0 -56 32 -17 79 -16 98 0 24 21 107 -5 115 -36 4 -17 8 -19 18 -9 10
10 10 17 0 32 -6 11 -12 30 -12 44 0 13 -7 29 -16 37 -14 11 -13 15 8 31 13 9
40 18 60 18 34 0 39 -4 53 -36 18 -45 20 -133 3 -139 -7 -3 -35 -6 -62 -9 -37
-2 -55 -9 -69 -27 -21 -25 -34 -26 -82 -4 -16 7 -58 16 -92 20 -55 6 -64 4
-83 -15 -19 -19 -21 -31 -18 -86 2 -35 0 -64 -4 -64 -11 0 -28 62 -28 103 0
22 -7 63 -15 91 -27 96 -45 185 -45 226 l0 40 76 0 c64 0 84 -5 137 -31z m60
-146 c20 -12 2 -23 -38 -23 -32 0 -45 4 -45 14 0 8 6 16 13 19 12 5 54 -1 70
-10z m248 -167 c28 -25 71 -52 99 -61 28 -9 50 -20 50 -23 0 -4 -26 -15 -57
-24 -50 -14 -60 -15 -84 -2 -16 8 -38 14 -51 14 -14 0 -41 18 -67 45 -43 43
-44 45 -27 64 10 11 30 22 45 24 14 2 30 5 35 6 4 0 30 -19 57 -43z m-238 2
c69 -22 144 -91 159 -144 13 -49 4 -56 -48 -34 -51 22 -64 24 -64 10 0 -10 84
-50 106 -50 26 0 16 -48 -13 -67 -16 -10 -30 -20 -33 -24 -3 -3 -14 -8 -25
-12 -11 -3 -53 -19 -94 -36 -87 -37 -106 -38 -125 -8 -15 25 -32 323 -21 370
7 25 9 26 54 20 25 -3 72 -14 104 -25z m258 -144 c14 -12 15 -22 8 -50 -6 -19
-7 -39 -4 -44 4 -6 0 -17 -8 -25 -21 -20 -73 -5 -69 21 5 40 5 50 -3 88 l-8
39 33 -7 c19 -3 41 -13 51 -22z m473 -1235 c-3 -5 -10 -7 -15 -3 -5 3 -7 10
-3 15 3 5 10 7 15 3 5 -3 7 -10 3 -15z"/>
</g>
  </g>
  <path id="rebuildPath"/>
  <path id="tracePath"/>
  <g id="armLayer"></g>
  <circle id="armHead" r="3.5"/>
  <script><![CDATA[
(() => {
  const SAMPLE_COUNT = 256;
  const ARM_COUNT = 64;
  const DURATION_MS = 2000;
  const HOLD_MS = 0;
  const CYCLE_MS = DURATION_MS + HOLD_MS;
  const DFT_MODE = "loops";
  const FIXED_CENTER = false;
  const CENTER_POINT = {"x":360,"y":360};
  const TAU = Math.PI * 2;
  const svg = document.documentElement;
  const sourceLayer = document.getElementById("sourceLayer");
  const armLayer = document.getElementById("armLayer");
  const rebuildPath = document.getElementById("rebuildPath");
  const tracePath = document.getElementById("tracePath");
  const armHead = document.getElementById("armHead");

  function transformPoint(point, matrix) {
    const svgPoint = svg.createSVGPoint();
    svgPoint.x = point.x;
    svgPoint.y = point.y;
    const transformed = svgPoint.matrixTransform(matrix);
    return { x: transformed.x, y: transformed.y };
  }

  function splitPathDataIntoLoops(d) {
    const tokens = String(d || "").match(/[AaCcHhLlMmQqSsTtVvZz]|[-+]?(?:\d*\.\d+|\d+\.?)(?:[eE][-+]?\d+)?/g) || [];
    const paramCounts = { A: 7, C: 6, H: 1, L: 2, M: 2, Q: 4, S: 4, T: 2, V: 1 };
    const loops = [];
    let chunk = [];
    let command = "";
    let index = 0;
    let current = { x: 0, y: 0 };
    let subpathStart = { x: 0, y: 0 };

    const isCommand = (token) => /^[A-Za-z]$/.test(token);
    const numberAt = () => Number(tokens[index++]);
    const format = (value) => {
      const rounded = Math.abs(value) < 1e-9 ? 0 : value;
      return Number(rounded.toFixed(6)).toString();
    };
    const push = (cmd, values = []) => {
      chunk.push(values.length ? cmd + " " + values.map(format).join(" ") : cmd);
    };
    const finish = () => {
      if (chunk.length > 0) {
        loops.push(chunk.join(" "));
        chunk = [];
      }
    };
    const updateEndpoint = (upper, relative, values) => {
      const relPoint = (x, y) => relative ? { x: current.x + x, y: current.y + y } : { x, y };
      if (upper === "L") current = relPoint(values[0], values[1]);
      else if (upper === "H") current = { x: relative ? current.x + values[0] : values[0], y: current.y };
      else if (upper === "V") current = { x: current.x, y: relative ? current.y + values[0] : values[0] };
      else if (upper === "C") current = relPoint(values[4], values[5]);
      else if (upper === "S" || upper === "Q") current = relPoint(values[2], values[3]);
      else if (upper === "T") current = relPoint(values[0], values[1]);
      else if (upper === "A") current = relPoint(values[5], values[6]);
    };

    while (index < tokens.length) {
      if (isCommand(tokens[index])) {
        command = tokens[index++];
      } else if (!command) {
        break;
      }

      const upper = command.toUpperCase();
      const relative = command !== upper;

      if (upper === "Z") {
        push("Z");
        current = { ...subpathStart };
        finish();
        command = "";
        continue;
      }

      if (upper === "M") {
        finish();
        if (index + 1 >= tokens.length) break;
        let x = numberAt();
        let y = numberAt();
        if (relative) {
          x += current.x;
          y += current.y;
        }
        current = { x, y };
        subpathStart = { x, y };
        push("M", [x, y]);

        while (index + 1 < tokens.length && !isCommand(tokens[index])) {
          const lx = numberAt();
          const ly = numberAt();
          if (relative) {
            push("l", [lx, ly]);
            current = { x: current.x + lx, y: current.y + ly };
          } else {
            push("L", [lx, ly]);
            current = { x: lx, y: ly };
          }
        }
        command = relative ? "l" : "L";
        continue;
      }

      const count = paramCounts[upper];
      if (!count) break;

      while (index < tokens.length && !isCommand(tokens[index])) {
        if (index + count > tokens.length) break;
        const values = [];
        for (let i = 0; i < count; ++i) {
          if (isCommand(tokens[index])) break;
          values.push(numberAt());
        }
        if (values.length !== count) break;
        push(command, values);
        updateEndpoint(upper, relative, values);
      }
    }

    finish();
    return loops;
  }

  function samplePaths() {
    const paths = Array.from(sourceLayer.querySelectorAll("path"));
    const loops = [];
    let totalLength = 0;

    for (const path of paths) {
      for (const d of splitPathDataIntoLoops(path.getAttribute("d"))) {
        const loopPath = document.createElementNS("http://www.w3.org/2000/svg", "path");
        loopPath.setAttribute("d", d);
        const parent = path.parentNode || sourceLayer;
        parent.appendChild(loopPath);

        let length = 0;
        try {
          length = loopPath.getTotalLength();
        } catch (err) {
          length = 0;
        }
        if (length <= 0) {
          loopPath.remove();
          continue;
        }

        loopPath.setAttribute("opacity", "0");
        loopPath.setAttribute("fill", "none");
        const matrix = loopPath.getCTM();
        if (!matrix) {
          loopPath.remove();
          continue;
        }

        loops.push({ path: loopPath, length, matrix });
        totalLength += length;
      }
    }

    if (!loops.length || totalLength <= 0) return [];

    return loops.map((loop) => {
      const loopSamples = Math.max(64, Math.round(SAMPLE_COUNT * loop.length / totalLength));
      const points = [];
      for (let i = 0; i < loopSamples; ++i) {
        const distance = loop.length * i / loopSamples;
        points.push(transformPoint(loop.path.getPointAtLength(distance), loop.matrix));
      }
      return { points, length: loop.length };
    }).filter((loop) => loop.points.length >= 2);
  }

  function stitchLoops(loops) {
    const sourceLoops = loops.filter((loop) => loop.points.length >= 2);
    if (!sourceLoops.length) return [];

    const totalLength = sourceLoops.reduce((sum, loop) => sum + loop.length, 0);
    const targetCount = Math.max(2, SAMPLE_COUNT);
    const points = [];

    for (const loop of sourceLoops) {
      const desired = Math.max(2, Math.round(targetCount * loop.length / Math.max(1, totalLength)));
      for (let i = 0; i < desired && points.length < targetCount; ++i) {
        const index = Math.min(loop.points.length - 1, Math.floor(i * loop.points.length / desired));
        points.push(loop.points[index]);
      }
    }

    return points.length >= 2 ? [{ points, length: totalLength }] : [];
  }

  function prepareLoops(loops) {
    let totalLength = 0;
    const dftLoops = DFT_MODE === "single" ? stitchLoops(loops) : loops;
    const prepared = dftLoops.map((loop) => {
      const coeffs = orderedCoefficients(computeDft(loop.points)).slice(0, Math.min(ARM_COUNT + 1, loop.points.length));
      const trace = loop.points.map((_, index) => reconstructAt(index / loop.points.length, coeffs));
      totalLength += loop.length;
      return { ...loop, coeffs, trace, start: 0, end: 0 };
    });

    let cursor = 0;
    for (const loop of prepared) {
      loop.start = cursor / Math.max(1, totalLength);
      cursor += loop.length;
      loop.end = cursor / Math.max(1, totalLength);
    }

    return prepared;
  }

  function computeDft(points) {
    const n = points.length;
    const half = Math.floor(n / 2);
    const coeffs = [];
    for (let k = 0; k < n; ++k) {
      const freq = k <= half ? k : k - n;
      let re = 0;
      let im = 0;
      for (let i = 0; i < n; ++i) {
        const angle = -TAU * freq * i / n;
        const cos = Math.cos(angle);
        const sin = Math.sin(angle);
        const x = points[i].x;
        const y = points[i].y;
        re += x * cos - y * sin;
        im += x * sin + y * cos;
      }
      re /= n;
      im /= n;
      coeffs.push({ freq, re, im, amp: Math.hypot(re, im) });
    }
    return coeffs;
  }

  function orderedCoefficients(coeffs) {
    const dc = coeffs.find((coef) => coef.freq === 0);
    const rest = coeffs.filter((coef) => coef.freq !== 0).sort((a, b) => b.amp - a.amp);
    return dc ? [dc, ...rest] : rest;
  }

  function reconstructAt(t, coeffs) {
    let x = FIXED_CENTER ? CENTER_POINT.x : 0;
    let y = FIXED_CENTER ? CENTER_POINT.y : 0;
    for (const coef of coeffs) {
      if (FIXED_CENTER && coef.freq === 0) continue;
      const angle = TAU * coef.freq * t;
      const cos = Math.cos(angle);
      const sin = Math.sin(angle);
      x += coef.re * cos - coef.im * sin;
      y += coef.re * sin + coef.im * cos;
    }
    return { x, y };
  }

  function epicycleAt(t, coeffs) {
    const points = [];
    const dc = coeffs.find((coef) => coef.freq === 0);
    let x = FIXED_CENTER ? CENTER_POINT.x : (dc ? dc.re : 0);
    let y = FIXED_CENTER ? CENTER_POINT.y : (dc ? dc.im : 0);
    points.push({ x, y, radius: 0 });

    for (const coef of coeffs) {
      if (coef.freq === 0) continue;
      const angle = TAU * coef.freq * t;
      const cos = Math.cos(angle);
      const sin = Math.sin(angle);
      const nextX = x + coef.re * cos - coef.im * sin;
      const nextY = y + coef.re * sin + coef.im * cos;
      points.push({ x: nextX, y: nextY, fromX: x, fromY: y, radius: coef.amp });
      x = nextX;
      y = nextY;
    }
    return points;
  }

  function pathData(points, limit, close) {
    if (points.length < 2) return "";
    const count = Math.max(2, Math.min(points.length, limit ?? points.length));
    let data = "M " + points[0].x.toFixed(2) + " " + points[0].y.toFixed(2);
    for (let i = 1; i < count; ++i) {
      data += " L " + points[i].x.toFixed(2) + " " + points[i].y.toFixed(2);
    }
    return close && count >= points.length ? data + " Z" : data;
  }

  function createArmElements(loopCount, count) {
    const loopElements = [];
    for (let loopIndex = 0; loopIndex < loopCount; ++loopIndex) {
      const group = document.createElementNS("http://www.w3.org/2000/svg", "g");
      armLayer.appendChild(group);
      const arms = [];
      for (let i = 0; i < count; ++i) {
        const circle = document.createElementNS("http://www.w3.org/2000/svg", "circle");
        const line = document.createElementNS("http://www.w3.org/2000/svg", "line");
        circle.setAttribute("class", "arm-circle");
        line.setAttribute("class", "arm-line");
        group.append(circle, line);
        arms.push({ circle, line });
      }
      loopElements.push(arms);
    }
    return loopElements;
  }

  function syncArms(chain, armElements) {
    for (let i = 1; i < chain.length; ++i) {
      const arm = chain[i];
      const el = armElements[i - 1];
      if (!el) continue;
      el.circle.setAttribute("cx", arm.fromX.toFixed(2));
      el.circle.setAttribute("cy", arm.fromY.toFixed(2));
      el.circle.setAttribute("r", Math.max(0, arm.radius).toFixed(2));
      el.line.setAttribute("x1", arm.fromX.toFixed(2));
      el.line.setAttribute("y1", arm.fromY.toFixed(2));
      el.line.setAttribute("x2", arm.x.toFixed(2));
      el.line.setAttribute("y2", arm.y.toFixed(2));
    }
  }

  const loops = prepareLoops(samplePaths());
  if (!loops.length) return;

  const maxArms = Math.max(...loops.map((loop) => loop.coeffs.length - 1));
  const armElements = createArmElements(loops.length, maxArms);
  rebuildPath.setAttribute("d", loops.map((loop) => pathData(loop.trace, loop.trace.length, true)).join(" "));

  let start = null;
  function frame(now) {
    if (start === null) start = now;
    const cycleTime = (now - start) % CYCLE_MS;
    const t = Math.min(1, cycleTime / DURATION_MS);
    const drawnPaths = [];
    let activeEnd = null;

    for (let loopIndex = 0; loopIndex < loops.length; ++loopIndex) {
      const loop = loops[loopIndex];
      if (t >= loop.end) {
        drawnPaths.push(pathData(loop.trace, loop.trace.length, true));
        continue;
      }
      if (t >= loop.start && t < loop.end) {
        const localT = (t - loop.start) / Math.max(0.0001, loop.end - loop.start);
        drawnPaths.push(pathData(loop.trace, Math.floor(localT * loop.trace.length) + 2, false));
        const chain = epicycleAt(localT, loop.coeffs);
        syncArms(chain, armElements[loopIndex]);
        activeEnd = chain[chain.length - 1];
      }
    }

    if (!activeEnd) {
      const loopIndex = loops.length - 1;
      const chain = epicycleAt(1, loops[loopIndex].coeffs);
      syncArms(chain, armElements[loopIndex]);
      activeEnd = chain[chain.length - 1];
    }

    armHead.setAttribute("cx", activeEnd.x.toFixed(2));
    armHead.setAttribute("cy", activeEnd.y.toFixed(2));
    tracePath.setAttribute("d", drawnPaths.join(" "));
    requestAnimationFrame(frame);
  }

  requestAnimationFrame(frame);
})();
  ]]></script>
</svg>
